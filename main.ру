import os
import asyncio
import threading
import re
from http.server import HTTPServer, BaseHTTPRequestHandler
from telethon import TelegramClient, events
from neonize.client import NewClient
from neonize.events import MessageEv

# --- СЕРВЕР ДЛЯ RENDER ---
class HealthCheckHandler(BaseHTTPRequestHandler):
    def do_GET(self):
        self.send_response(200)
        self.end_headers()
        self.wfile.write(b"Aaron System Live")

def run_health_server():
    port = int(os.environ.get("PORT", 8080))
    server = HTTPServer(('0.0.0.0', port), HealthCheckHandler)
    server.serve_forever()

# --- НАСТРОЙКИ ---
# Твой API ID: 30893125, API Hash: 5c73de68f3152f0513292cb495071e0b
API_ID = 30893125 
API_HASH = '5c73de68f3152f0513292cb495071e0b'
PHONE = os.environ.get('PHONE_NUMBER', '66618169428')

tg_client = TelegramClient('aaron_session', API_ID, API_HASH)
wa_client = NewClient("db.sqlite3")

# --- СТАТИСТИКА (Твои точки + Касса) ---
stats = {
    "tg_in": 0, "wa_in": 0,
    "dots_count": 0,
    "money_thb": 0, "money_usd": 0
}

def get_ai_level():
    total = stats["tg_in"] + stats["wa_in"]
    if total < 15: return "Уровень 1: База"
    if total < 50: return "Уровень 2: Аналитик"
    return "Уровень 3: Максимум ИИ"

# --- ОБРАБОТЧИКИ ---
@wa_client.event(MessageEv)
def on_wa_message(client, message: MessageEv):
    if not message.Info.FromMe:
        stats["wa_in"] += 1
        text = (message.Message.conversation or "").lower()
        stats["money_thb"] += sum(int(m[0]) for m in re.findall(r'(\d{2,6})\s?(thb|бат)', text))
        stats["money_usd"] += sum(int(m[0]) for m in re.findall(r'(\d{1,5})\s?(\$|usd)', text))
    elif message.Info.FromMe and message.Message.conversation == ".":
        stats["dots_count"] += 1

@tg_client.on(events.NewMessage)
async def on_tg_message(event):
    if not event.out and event.is_private:
        stats["tg_in"] += 1
        text = event.raw_text.lower()
        stats["money_thb"] += sum(int(m[0]) for m in re.findall(r'(\d{2,6})\s?(thb|бат)', text))
        stats["money_usd"] += sum(int(m[0]) for m in re.findall(r'(\d{1,5})\s?(\$|usd)', text))
    elif event.out and event.raw_text == ".":
        stats["dots_count"] += 1
        await event.delete()
        report = (
            f"📈 **ОТЧЕТ ААРОНА**\n"
            f"🧠 {get_ai_level()}\n"
            f"📍 Твоих точек: {stats['dots_count']}\n"
            f"💬 Всего сообщений: {stats['tg_in'] + stats['wa_in']}\n"
            f"💰 Касса: {stats['money_thb']} THB / {stats['money_usd']} USD"
        )
        await event.respond(report)

# --- ЗАПУСК БЕЗ ЗАПРОСОВ В КОНСОЛЬ ---
async def start():
    threading.Thread(target=run_health_server, daemon=True).start()
    # Авторизация Telegram через сохраненную сессию
    await tg_client.start(phone=lambda: PHONE)
    
    if not wa_client.is_connected():
        print(f"📲 ГЕНЕРАЦИЯ КОДА ДЛЯ {PHONE}...")
        wa_client.pair_phone(PHONE)
        
    print("✅ СИСТЕМА ЗАПУЩЕНА И ЖДЕТ КОД")
    await tg_client.run_until_disconnected()

if __name__ == '__main__':
    asyncio.run(start())
